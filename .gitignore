# .NET
bin/
obj/
.vs/
*.user
*.suo

# Android / Gradle
.gradle/
build/
.idea/
.kotlin/
local.properties
*.apk
*.aab

# Misc
*.log
Thumbs.db
.DS_Store

# Build output / staging / release
Staging_Tester/
release/
_installer_out/
*.zip
*.exe
*.dll
*.pdb

# Database (real data)
*.db
*.db-shm
*.db-wal

# Secrets
appsettings.json
appsettings.*.json
# ...but keep the secret-free template so the required config shape is documented in git
!appsettings.example.json

# Licensing - DO NOT publish
license_keys*.csv
keys.csv
KeyGenTool/
LibraryLicenseIssuer/
# License SALT for offline hash+salt activation (shared secret, MUST match keygen).
# build-release.ps1 reads it from here to stage into the deployed appsettings.json.
*.secret
license.secret

# Installer build output (self-contained publish — large, regenerated by build-release.ps1)
admin-pc/installer/dist/
# Compiled installers (.exe produced by Inno Setup iscc)
admin-pc/installer/Output/
